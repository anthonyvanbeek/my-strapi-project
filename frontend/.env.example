# Public URL of the Strapi backend (defaults to localhost for local development)
STRAPI_API_URL=https://your-strapi-cloud-project.cloud.strapi.io

# Optional: API token with read permissions to the Article collection type.
# Leave empty if the Article type is publicly accessible.
STRAPI_API_TOKEN=
